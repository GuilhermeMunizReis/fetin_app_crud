import { SQL } from 'drizzle-orm/sql';
import { AnyMySqlColumn } from './columns';
import { AnyMySqlTable } from './table';
interface IndexConfig {
    name: string;
    columns: IndexColumn[];
    /**
     * If true, the index will be created as `create unique index` instead of `create index`.
     */
    unique?: boolean;
    /**
     * If set, the index will be created as `create index ... using { 'btree' | 'hash' }`.
     */
    using?: 'btree' | 'hash';
    /**
     * If set, the index will be created as `create index ... algorythm { 'default' | 'inplace' | 'copy' }`.
     */
    algorythm?: 'default' | 'inplace' | 'copy';
    /**
     * If set, adds locks to the index creation.
     */
    lock?: 'default' | 'none' | 'shared' | 'exclusive';
}
export declare type IndexColumn = AnyMySqlColumn | SQL;
export declare class IndexBuilderOn {
    private name;
    private unique;
    constructor(name: string, unique: boolean);
    on(...columns: [IndexColumn, ...IndexColumn[]]): IndexBuilder;
}
export interface AnyIndexBuilder {
    build(table: AnyMySqlTable): Index;
}
export interface IndexBuilder extends AnyIndexBuilder {
}
export declare class IndexBuilder implements AnyIndexBuilder {
    protected $brand: 'MySqlIndexBuilder';
    constructor(name: string, columns: IndexColumn[], unique: boolean);
    using(using: IndexConfig['using']): Omit<this, 'using'>;
    algorythm(algorythm: IndexConfig['algorythm']): Omit<this, 'algorythm'>;
    lock(lock: IndexConfig['lock']): Omit<this, 'lock'>;
}
export declare class Index {
    protected $brand: 'MySqlIndex';
    readonly config: IndexConfig & {
        table: AnyMySqlTable;
    };
    constructor(config: IndexConfig, table: AnyMySqlTable);
}
export declare type GetColumnsTableName<TColumns> = TColumns extends AnyMySqlColumn<{
    tableName: infer TTableName extends string;
}> | AnyMySqlColumn<{
    tableName: infer TTableName extends string;
}>[] ? TTableName : never;
export declare function index(name: string): IndexBuilderOn;
export declare function uniqueIndex(name: string): IndexBuilderOn;
export {};
//# sourceMappingURL=indexes.d.ts.map